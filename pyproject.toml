[build-system]
requires = ["scikit-build-core"]
build-backend = "scikit_build_core.build"

[project]
name = "PyVerse"
version = "0.1.0"
requires-python = ">=3.8"

[tool.scikit-build.wheel]
py-api = "cp38"