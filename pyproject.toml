[build-system]
requires = ["setuptools", "wheel", "cmake", "cibuildwheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PyVerse"
version = "0.1.0"
requires-python = ">=3.8"

[tool.cibuildwheel]
archs = ["AMD64"]