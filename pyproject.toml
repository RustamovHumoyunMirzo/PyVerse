[build-system]
requires = ["scikit-build", "cmake", "ninja"]
build-backend = "setuptools.build_meta"

[project]
name = "PyVerse"
version = "0.1.0"
requires-python = ">=3.8"
description = "PyVerse framework"
authors = [{name="Rustamov Humoyun Mirzo"}]